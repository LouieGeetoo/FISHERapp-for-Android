<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
		android:layout_width="match_parent"
    	android:id="@+id/lytTitleBar"
    	android:layout_height="wrap_content" android:background="@color/fisher_maroon" android:paddingTop="0dp" android:paddingBottom="2dp">
        <TextView android:layout_alignParentLeft="true"
        	android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:textSize="15sp"
        	android:paddingLeft="10dp"
        	android:id="@+id/txtTitle"
        	android:text="People" android:textColor="@android:color/white"/>
    </RelativeLayout>